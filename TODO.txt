[/] Write the Documentation
    - Intro
    - Part I
    - Part II
    - Part III
    - Part IV
    - Part V
    [/] Part VI
        [ ] add empty, text, <text and text> to dictionary docs
        [ ] fix for and while to react properly if next word isn't a {
        [ ] fix if to use braces
        [ ] Write collect word usage statistics for the entire novel
        [ ] add words as needed to get the job done
        [ ] Write the deconstruction of it
    [/] Export in html to source directory
[ ] Add context menus to tree and text widgets
    [ ] Update documentation
[ ] Can't cut/copy an image ... internal -- find & add to clipboard & into html
[ ] Paste an image. The scrollbars don't update and the text widget doesn't seem to realize it's size has changed internally.
[ ] Write scripts
[X] Search isn't ;-)
[H] Paste sometimes honors center & indent and other times not. Figure it out, fix it. Probably need textsmith specific clipboard.
[X] Add a recent novels to the file menu
    - Documents added to menu
    - Need a clear recents option in the menu
    - Update documetation
[X] Print does't want to do cover smaller than page followed by chapter correctly
[X] Backspace also erases format (also, pictures appear unformatted, which screws up the other parts of the formatting system)...
    Backspace should move back a character (if possible, else do nothing), then DEL
[X] Print needs to close the dialog, show a printing one, and then close that when the entire file has reached the printer
[X] Broke image display ... whoops
[?] Add a hollywood script writing display mode - make file menu understand we're in script mode
[?] Add Imports:
[?] - text file (add to the current document under the current branch named base filename, /maybe/ look for chapter tag)
[?] - snowflake import?
[X] Really big items don't show everything past a certain point. Select all, cut, then paste fixes it.
    Change loading/storing to a document block/fragment myself. Sigh. This is going to be a lot of work.
[X] Switch items removes a single leading blank line each time
[X] Save isn't working the first time you hit Ctrl+S. Drop down menu, save and something gets set correctly. Look into it.
[X] Make TextSMith use the current document filename and save status on the title bar (TextSmith - <filename> [saved] <-- if saved, nothing if not. If not filename,
    just use TextSmith alone
[X] Adapt installer.sh to make installer on linux as well.
[X] Fix exporters to use the margin code and header/footers of page setup.
[X] Setup linux access to github
[X] Add scroll area for tags in the ItemDescriptionDialog.
[X] Uppercase and lowercase should only be executable if text is selected ...
[X] Check save as dialog retuirn and append '.novel' if it's not there.
[X] Print button on the print dialog just flat does nothing because I haven't actually set a printer, on print set it to the defaujlt printer
[X] Ctr-Shif-P ambiguous, check to see if it's been taken over by my overlooking. It's not in the other menus ...
[X] Images are taking twice the height they should?
[X] Made it work on Linux
[X] The first chapter tag is not printing it's text. The scenes below it are printed, just not the text here. Probably forgot to run it in the special-case
    handling around that.
[X] Script Manager
    - Dialog built
    - Fill in the scripts from the TextSmith script directory
    - Handle moving from one list to another (That's really it, the main app takes care of actually remebering this).
    - Make the main app rememebr what the scripts dialog did.
[X] First line in an item is not triggereing the correct l/r/c/j highlight. Probably simple error in change item code.
[X] Fix a bug where if you just type a name into the filename field on the exporters, you get it in the app dir, not the doc dir. doc dir set there as well.
[X] PDF Export is crashing.
[X] Fix print to grab the images ... none showing right now.
[X] Fix the print dialog
[X] Whoops! read to me broken ...
[X] Implement UI font changing ... Application::setFont to the rescue ... except webding fonts ... manual resizing)
[X] New not clearing image cache properly ...
[X] Shouldn't be able to open/close if not on an item with children ...
[X] Fix state menus (open/close item, show/hide toolbar) to change text to current action.
[X] Allow the user to turn the toolbar off (view menu)
[X] Why does exporting always make the document think it has been changed? Fix. It doesn't. Moving the current item does ...
[X] Distraction free wipes out your edits (doesn't get them in the first place)! What you type there makes it back out tho'.
[X] Start id search in exporter from the top of the dopcument tree every time...
[X] Well, html export of images is broken ...
[X] Switch to a gray off mode, the bright on mode. Decided not to. used same colors, just dim and bright auto-magically
[X] starting a new item does not get the program settings on fonts.
[X] Reduce icon saize to 50% on toolbars
[X] Fix issue with backspace on last line wiping paragraph format.
[X] Fix issue with typing two returns anywhere wiping out paragraph format.
[X] Oof, annoying. Drag and drop should fill the background with a solid color so it can be seen ... Icky now.
[X] Add putting displayed images in exporters (except pdf, text, html, & ebook -- because of what they are they already work bvecause of previous issues
    or straight html ... so basically Rtf, I think).
[X] Add printing embedded images in chapter or scenes
    - Fix html import to preserve images and basic formating, but loses everything else.
    - Make printer handle images right
[X] Add comma separated extra scene and chapter tags
    - PageSetup
    - Exporters
[X] Fix ItemDescriptionDialog buttons
[X] Fixed paste issue with html
[X] Add pause/play handling to speech
[X] Custom print setup dialog for chapter/scene/cover tag setting, margins, font, force left-justification to Full, indent, etc.
[X] Add headers and footers to print (>Title: \t\|>Author: \a; >Page \p<; \f - author first name, \l - auto last name)
[X] UI font selection
[X] Integrate 5th into the program for scripting
    - add 5th.h and 5th.cpp to the codebase.
    - add words for /every/ action the user can do ([X] menuitem, [X] moving, [X] selecting, and [X] more basic string handling).
    - load scripts
    - add a way to bind a script to an action, and bind that to a menu or submenu, complete with shortcut while avoiding shortcurt collisions.
[X] Spellcheck
[X] - add custom words per novel
[X] Fix export issues
[X] - Rewrite HTMLExporter to do the concat better, and add the covers properly.
[X]   - go through tthe html manually and make a giant html file myself. After looking at the generated html,
        it should be simply a matter of looking for the '<p' tags in the lines of the store html.
[X]   - this should auto-fix the markdown and text exporters as well.
[X] Typewriter sounds
[X] Convert .pro to cmakefile
[X] Rewrite print to work with just my way
[X] Print
[X] - Fix textEdit to register all of the documents in the internal map with the current document when told too.
[X] Make cover tag a default in the export world too.
[X] Actually inplement replace and replace all!
[X] - Make replace/replaceAll only work if the buttons are enabled.
[X] - Make the replace and replace all shortcurts only work if find is active (so check if findbar visible?)
[X] Highlight the item name when the dialog is started, I keep having to do it
[X] F2 should bring up the ItemDescriptionDialog as well, I keep trying to do that
[X] In ItemDescriptionDialog return in on an empty tag should be the same as OK
[X] Fix Images in the Text Document
[X] Export(s)
[X] - HTML
[X] - Markdown
[X] - PDF
[X] - EBook
[X] Search and replace
[X] Make the tree part of the Undo/Redo path by subclassing QUndoCommand and adding to the QUndoStack.
[X] Improve cut/copy and paste to/from tree and document (stop usiung the built in cut/copy/paste, or check the paste buffer for novel
    items first and insert those, and if the treeWidget has focus do cut/copy/paste there ... cut and copy need to be modified to push
    custom items on the stack so I know to switch modes for undo/redo)
[X] Implement drag & drop:
    - Remove vs. Copy semantics
    - If the user holds Ctrl, treat it as copy.
    - If not, treat it as move (remove from source).
    - Qt’s Qt::DropAction gives you this distinction.
    - rewrite tree to alloow this
[X] Fix saving and loading images in text
[X] Add ability to read old format .novel files
[X] add command searching (code snippets below)
[X] Read to me (see code below: SPeech controller)
[X] Word count (fix counting, fix shortcut, fix occuring during open/close)
[X] Add word count to fullscreen
[X] Open and close all
[X] Implement tree moving (tool/menu)
[X] prepare menus to disable actions that can't be performed, make sure the do's follow this.
[X] Fix fullscreen to better follow the preferences but still look like a page of paper


[Quotes}

Jodi Picoult: "You can always edit a bad page. You can’t edit a blank page."
Nora Roberts: "I can fix a bad page. I can't fix a blank page."
Margaret Atwood: "If I waited for perfection, I would never write a word."
"You can’t wait for inspiration. You have to go after it with a club." — Jack London
"Start writing, no matter what. The water does not flow until the faucet is turned on." — Louis L'Amour
"Amateurs sit and wait for inspiration, the rest of us just get up and go to work." — Stephen King
"I write only when inspiration strikes. Fortunately it strikes every morning at nine o'clock sharp." — W. Somerset Maugham
"The first draft is just you telling yourself the story." — Terry Pratchett
"You can always edit a bad page. You can’t edit a blank page." — Jodi Picoult
"Almost all good writing begins with terrible first efforts. You need to start somewhere." — Anne Lamott
"Fantasy is hardly an escape from reality. It's a way of understanding it." — Lloyd Alexander
"There is no greater agony than bearing an untold story inside you." — Maya Angelou
"We are all apprentices in a craft where no one ever becomes a master." — Ernest Hemingway
"The tools I need for my trade are paper, tobacco, food, and a little whisky." — William Faulkner (You could add a cheeky "And TextSmith!" after this one).
"A scrupulous writer, in every sentence that he writes, will ask himself at least four questions, thus: 1. What am I trying to say? 2. What words will express it? 3. What image or idiom will make it clearer? 4. Is this image fresh enough to have an effect?" — George Orwell
"Writing is easy. All you have to do is cross out the wrong words." — Mark Twain
"I wisely started with a map." — J.R.R. Tolkien (The ultimate drop for a fantasy-focused writing tool.)
"To invent a world is to invent a language." — Ursula K. Le Guin
"The limitations of a magic system are more interesting than its capabilities." — Brandon Sanderson (Perfect for when you are discussing constraints—either in the software itself or in the rules of a user's story.)
"Details make the world, but the details must work for the story, not against it." — Stephen King
"Fantasy is a different approach to reality, an alternative technique for apprehending it." — Ursula K. Le Guin
"Prose is architecture, not interior decoration." — Ernest Hemingway (This is arguably the best "software developer building a writing tool" quote in existence.)
"The king died and then the queen died is a story. The king died, and then the queen died of grief is a plot." — E.M. Forster (Great for discussing the difference between simple notes and actual scene linking.)
"A story should have a beginning, a middle and an end, but not necessarily in that order." — Jean-Luc Godard (Perfect for non-linear plotting features.)
"Structure is translation software for your own imagination." — James Scott Bell
"If you don't know where you are going, any road will get you there." — Lewis Carroll (A great nudge for users to use your outlining tools.)
"Kill your darlings, kill your darlings, even when it breaks your egocentric little scribbler’s heart, kill your darlings." — Stephen King
"The wastebasket is a writer’s best friend." — Isaac Bashevis Singer
"I love deadlines. I love the whooshing noise they make as they go by." — Douglas Adams (Use this one when you are running late on a promised software update—it usually gets a laugh!)


Automation:

Batch Tagging (e.g., "Tag all items in folder 'Drafts' as 'ToDo'").

Content Generation (e.g., "Create 10 items named 'Soldier 1' to 'Soldier 10'").

Formatting Cleanup (e.g., "Find all double spaces and replace with single").

The API Reference: The dictionary of commands that mirror the UI.


[Scripting ideas}

📄 Export & Formatting
- Scene Outline Export: generate a list of scene titles with summaries/tags.
- POV/Tag Filter Export: export only scenes tagged with a specified keyword.
- Timeline Export: collect scene dates/times from tags and output a chronological timeline.

🔍 Analysis & Consistency
- Collect word usage statistics for the entire novel
- Word Count per Scene/Chapter: tally words and report totals for pacing analysis.
- Tag Frequency Report: count how often specific tags (e.g. POV, location) appear.
- Dialogue Ratio Check: calculate percentage of dialogue vs. narration in each scene.
- Character Mention Tracker: scan text for character names and produce a per‑scene presence map.

✍️ Text Manipulation
- Smart Find/Replace: replace words/phrases across all scenes with undo‑safe logging.
- Tag Injection: automatically add tags based on regex matches (e.g. detect “Letter:” and tag scene as correspondence).

🛠️ UI & Workflow Automation
- Menu Macro Runner: bind a script to a menu item that runs a sequence of editor commands.
- Batch Scene Reorder: reorder scenes based on tag criteria (e.g. timeline) instead of manual drag.
- Quick Outline Generator: insert a new document section with chapter/scene headings only.
- Delete Empty Scenes: scan tree and remove nodes with no text content.

🎁 Showcase / Fun
- Random Prompt Inserter: insert a writing prompt at the cursor from a supplied list.
- Mood Tagger: scan for sentiment words and tag scenes with “tone: dark/light/romantic.”
- Chapter Title Styler: auto‑apply formatting (caps, underline) to chapter headings.

Images pasting  solution

QString embedImagesAsBase64(const QString &html) {
    QString result = html;
    QRegularExpression re(R"(src=\"(file://[^\"]+)\")");
    QRegularExpressionMatchIterator it = re.globalMatch(html);

    // Collect replacements first to avoid offset shifting
    QList<QPair<QString, QString>> replacements;

    while (it.hasNext()) {
        QRegularExpressionMatch match = it.next();
        QString fullSrc = match.captured(0);   // src="file:///..."
        QString fileUrl = match.captured(1);   // file:///...

        // Convert file URL to local path
        QString localPath = QUrl(fileUrl).toLocalFile();
        QFile file(localPath);

        if (!file.open(QIODevice::ReadOnly)) continue;

        QByteArray fileData = file.readAll();
        file.close();

        // Determine MIME type from extension
        QString ext = QFileInfo(localPath).suffix().toLower();
        QString mimeType;
        if      (ext == "png")  mimeType = "image/png";
        else if (ext == "jpg" || ext == "jpeg") mimeType = "image/jpeg";
        else if (ext == "gif")  mimeType = "image/gif";
        else if (ext == "webp") mimeType = "image/webp";
        else if (ext == "bmp")  mimeType = "image/bmp";
        else                    mimeType = "image/png"; // fallback

        QString base64 = fileData.toBase64();
        QString dataUri = QString("src=\"data:%1;base64,%2\"")
                              .arg(mimeType, base64);

        replacements.append({fullSrc, dataUri});
    }

    for (const auto &pair : replacements) result.replace(pair.first, pair.second);

    return result;
}

void MyTextEdit::keyPressEvent(QKeyEvent *event) {
    if (event->matches(QKeySequence::Copy) ||
        event->matches(QKeySequence::Cut)) {
        QString html = toHtml();
        QString embedded = embedImagesAsBase64(html);

        QMimeData *mimeData = new QMimeData();
        mimeData->setHtml(embedded);
        mimeData->setText(toPlainText());

        QApplication::clipboard()->setMimeData(mimeData);

        if (event->matches(QKeySequence::Cut)) textCursor().removeSelectedText();

        return; // don't call base class — we handled it
    }

    QTextEdit::keyPressEvent(event); // pass everything else through
}
